-- В вызовах функций, определённых пользователем, 
-- типы формальных и фактических аргументов должны совпадать.
add10(x : int, y : int) : int = x + y;
addPoly10(xs : [int], ys : bool) : [int] = add10(xs, ys);